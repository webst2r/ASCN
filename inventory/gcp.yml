---
all:
 vars:
  
  # GKE cluster variables
  gcp_project: ascn-tp-g17           # ID of GCP Project
  gcp_auth_kind: serviceaccount            # Do not change
  gcp_cred_file: /home/daniel/Desktop/ASCN/ascn-credentials.json  # Path to service account keys (json file downloaded from GCP)

  gcp_zone: us-central1-a
  gcp_image_type: ubuntu_containerd
  gcp_machine_type: e2-small          # Can be changed if necessary
  gcp_disk_size_gb: 100                # Can be changed if necessary
  gcp_initial_node_count: 2            # Number of nodes to create. Can be changed if necessary


  # APP variables
  ghost_ip: 35.239.139.139  # Needs to be updated
  ghost_port: 2368       # Needs to be updated


  # Additional variables
  app : ghost
  app_image: ghost:latest
  
  ghost_db_name: ghost
  ghost_db_user: ghostuser
  ghost_db_password: "123456" 
